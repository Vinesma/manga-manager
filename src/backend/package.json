{
    "name": "backend",
    "private": true,
    "version": "0.0.0",
    "type": "commonjs",
    "main": "server.ts",
    "scripts": {
      "start": "ts-node server.ts",
      "dev": "nodemon",
      "build": "tsc --project ./",
      "test": "echo \"Error: no test specified\" && exit 1"
    }
  }

